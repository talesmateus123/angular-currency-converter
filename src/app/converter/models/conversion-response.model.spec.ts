import { ConversionResponse } from './conversion-response.model';

describe('ConversionResponse.Model', () => {
  it('should create an instance', () => {
    expect(new ConversionResponse()).toBeTruthy();
  });
});
